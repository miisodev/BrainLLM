{
  "manifest_version": "0.3",
  "name": "brainllm",
  "display_name": "BrainLLM",
  "version": "12.4.0",
  "description": "Turns TriliumNext Notes into a persistent, graph-structured second brain for Claude and other LLM clients.",
  "long_description": "BrainLLM is an MCP server that organises memory into five purpose-built areas (Master, LLM, Memory, Knowledge, Insights). The model supplies content while the server owns placement, format, lifecycle, dates and backups. Includes a full raw ETAPI surface for advanced use. Requires a running TriliumNext instance and the Bun runtime.",
  "author": {
    "name": "Kevin Miiso",
    "email": "miisodev@gmail.com",
    "url": "https://github.com/miisodev"
  },
  "homepage": "https://github.com/miisodev/BrainLLM#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/miisodev/BrainLLM.git"
  },
  "support": "https://github.com/miisodev/BrainLLM/issues",
  "license": "MIT",
  "keywords": ["mcp", "trilium", "trilium-next", "second-brain", "knowledge-graph", "memory", "claude"],
  "icon": "public/BrainLLM.png",
  "server": {
    "type": "binary",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "bun",
      "args": ["run", "${__dirname}/dist/index.js"],
      "env": {
        "TRILIUM_BASE_URL": "${user_config.trilium_base_url}",
        "TRILIUM_ETAPI_TOKEN": "${user_config.trilium_etapi_token}",
        "TRILIUM_PASSWORD": "${user_config.trilium_password}",
        "BRAINLLM_MODE": "${user_config.brainllm_mode}",
        "BRAINLLM_TZ": "${user_config.brainllm_tz}"
      }
    }
  },
  "tools_generated": true,
  "user_config": {
    "trilium_base_url": {
      "type": "string",
      "title": "Trilium base URL",
      "description": "URL of your TriliumNext instance.",
      "default": "http://localhost:8080",
      "required": true
    },
    "trilium_etapi_token": {
      "type": "string",
      "title": "Trilium ETAPI token (optional)",
      "description": "Preferred: create one in Trilium under Options -> ETAPI. Leave blank when using the password alternative.",
      "required": false,
      "sensitive": true
    },
    "trilium_password": {
      "type": "string",
      "title": "Trilium password (alternative)",
      "description": "Used only when no ETAPI token is supplied; BrainLLM mints and caches a token on first start.",
      "required": false,
      "sensitive": true
    },
    "brainllm_mode": {
      "type": "string",
      "title": "Tool surface",
      "description": "core (default) or full (adds the 33 raw ETAPI tools).",
      "default": "core",
      "required": false
    },
    "brainllm_tz": {
      "type": "string",
      "title": "Timezone (IANA)",
      "description": "Optional. e.g. America/New_York. Leave blank to use the host clock.",
      "required": false
    }
  },
  "compatibility": {
    "platforms": ["darwin", "win32", "linux"]
  }
}

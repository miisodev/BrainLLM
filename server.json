{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "io.github.miisodev/brainllm",
  "description": "Persistent, graph-structured memory for LLMs, stored in TriliumNext Notes. Enforced note structure, a session protocol with a durable pre-close gate, a typed knowledge graph, and a maintenance lifecycle — in a note application the human can read and edit too.",
  "version": "10.0.0",
  "websiteUrl": "https://github.com/miisodev/BrainLLM#readme",
  "repository": {
    "url": "https://github.com/miisodev/BrainLLM",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "npm",
      "identifier": "brainllm",
      "version": "10.0.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "TRILIUM_BASE_URL",
          "description": "Base URL of the TriliumNext instance, e.g. http://localhost:8080",
          "isRequired": true,
          "format": "string"
        },
        {
          "name": "TRILIUM_ETAPI_TOKEN",
          "description": "ETAPI token. Required unless TRILIUM_PASSWORD is set. Create one in Trilium under Options → ETAPI.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "TRILIUM_PASSWORD",
          "description": "Alternative to TRILIUM_ETAPI_TOKEN — BrainLLM mints a token on first start and caches it beside brainllm.json.",
          "isRequired": false,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "BRAINLLM_MODE",
          "description": "core (default) exposes the 38 brain-aware tools; full additionally exposes the 33 raw ETAPI tools.",
          "isRequired": false,
          "format": "string",
          "default": "core"
        },
        {
          "name": "BRAINLLM_TZ",
          "description": "IANA timezone used to stamp dated notes, e.g. Africa/Johannesburg. Defaults to the host timezone.",
          "isRequired": false,
          "format": "string"
        }
      ]
    }
  ]
}
